# ====================================================================
# NEXUS CORE - Backend Dependencies
# ====================================================================
# ESTRATEGIA DE VERSIONAMENTO:
# Usamos ranges flexiveis (>=) em vez de pinning exato (==).
# MOTIVO: Com "==" qualquer nova versao lancada no PyPI quebra o
#         `pip install -r requirements.txt`. Com ">=" o pip resolve
#         automaticamente para a versao mais recente compativel,
#         eliminando a necessidade de editar este arquivo no futuro.
#
# Para garantir reproducibilidade, gere um lock file com:
#     pip freeze > requirements.lock.txt
# ====================================================================

# --- Web framework ---
fastapi>=0.116.1
uvicorn[standard]>=0.35.0

# --- Banco de dados / ORM ---
sqlalchemy>=2.0.41
psycopg2-binary>=2.9.10
alembic>=1.16.4

# --- Autenticacao / Seguranca ---
python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.20

# --- Configuracao / Validacao ---
python-dotenv>=1.1.1
pydantic[email]>=2.11.7

# --- Cache / Filas ---
redis>=6.2.0

# --- Testes ---
pytest>=8.4.1
httpx>=0.28.1
