# ====================================================================
# NEXUS CORE - Backend Environment Variables (EXEMPLO)
# ====================================================================
# Copie este arquivo para ".env" e preencha com seus valores reais:
#     cp .env.example .env
#
# OBS: O arquivo ".env" NAO deve ser versionado (esta no .gitignore).
#      Use este .env.example como modelo/documentacao.
#
# IMPORTANTE: Nao adicione BOM (byte order mark) nem espacos antes
#      do nome da variavel. Use encoding UTF-8 sem BOM ou ASCII.
# ====================================================================

# --- Banco de dados PostgreSQL (usado pelo docker-compose) ---
# Para rodar localmente, use localhost; no docker-compose, use "db".
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/nexus

# --- Autenticacao / JWT ---
# Gere um valor forte com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=TroquePorUmaChaveMuitoGrandeEUnica

ALGORITHM=HS256

# Tempo de expiracao do token em minutos (padrao: 60)
ACCESS_TOKEN_EXPIRE_MINUTES=60
